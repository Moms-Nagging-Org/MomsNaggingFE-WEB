import CustomLayout from '@/components/Layout';

const Develop = () => {
  return (
    <CustomLayout data={''} open="/develop" title="개발중">
      개발중
    </CustomLayout>
  );
};

export default Develop;
