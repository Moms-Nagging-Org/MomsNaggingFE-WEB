import * as Styled from './styled';
import SideBarLogo from '../../assets/SideBarLogo';

const SideBar = () => {
  return (
    <Styled.Wrapper>
      <SideBarLogo />
    </Styled.Wrapper>
  );
};

export default SideBar;
